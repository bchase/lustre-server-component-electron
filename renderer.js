// import {main} from './app.mjs';

// document.addEventListener('DOMContentLoaded', () => {
//   const timezone = Intl.DateTimeFormat().resolvedOptions().timeZone;

//   // start the Gleam app
//   main(timezone);
// });

// document.addEventListener('clear-input', (evt) => {
//   const input = document.getElementById(evt.detail.id);

//   input.value = '';
// });

// window.js.listen((data) => {
//   window.dispatchEvent(new CustomEvent('rushtag.listen', data));
// });
